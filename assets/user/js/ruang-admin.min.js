(function ($) {
  "use strict"; // Start of use strict

  // Toggle the side navigation

    if ($(window).width() > 768) {

      $("#sidebarToggle, #sidebarToggleTop").on('click', function (e) {
        $("body").toggleClass("sidebar-toggled");

        $(".sidebar").toggleClass("toggled");

        var div = $('.sidebar');

        if (div.css('width') === '250px') {
          $('#wrapper #content-wrapper').animate({
            paddingLeft: '104px',
          }, 470);
          $('.topbar').animate({
            paddingLeft: '114px',
          }, 470);
        }

        var div2 = $('#wrapper #content-wrapper');
        if (div2.css('paddingLeft') === '104px') {
          $('#wrapper #content-wrapper').animate({
            paddingLeft: '250px',
          }, 500);
          $('.topbar').animate({
            paddingLeft: '260px',
          }, 500);
        }


        if ($(".sidebar").hasClass("toggled")) {
          $('.sidebar .collapse').collapse('hide');
        };

      });

    }
    if ($(window).width() <= 768) {
      $(".topbar #sidebarToggleTop ").on('click', function (e) {

        var div = $('.sidebar');
        if (div.css('left') == '-250px') {
          $('.sidebar').animate({
            left: '0px',
          });
          $('.topbar').animate({
            paddingLeft: '260px',
          }, 1000);
        }

        if (div.css('left') == '0px') {
          $('.sidebar').animate({
            left: '-250px',
          });
          $('.topbar').animate({
            paddingLeft: '10px',
          }, 1500);
        }

      });
    }



  // Close any open menu accordions when window is resized below 768px
  $(window).resize(function () {
    if ($(window).width() < 768) {
      $('.sidebar .collapse').collapse('hide');
    };
  });

  // Prevent the content wrapper from scrolling when the fixed side navigation hovered over
  $('body.fixed-nav .sidebar').on('mousewheel DOMMouseScroll wheel', function (e) {
    if ($(window).width() > 768) {
      var e0 = e.originalEvent,
        delta = e0.wheelDelta || -e0.detail;
      this.scrollTop += (delta < 0 ? 1 : -1) * 30;
      e.preventDefault();
    }
  });

  // Scroll to top button appear
  $(document).on('scroll', function () {
    var scrollDistance = $(this).scrollTop();
    if (scrollDistance > 100) {
      $('.scroll-to-top').fadeIn();
    } else {
      $('.scroll-to-top').fadeOut();
    }
  });

  // Smooth scrolling using jQuery easing
  $(document).on('click', 'a.scroll-to-top', function (e) {
    var $anchor = $(this);
    $('html, body').stop().animate({
      scrollTop: ($($anchor.attr('href')).offset().top)
    }, 1000, 'easeInOutExpo');
    e.preventDefault();
  });

})(jQuery); // End of use strict

// Modal Javascript

$(document).ready(function () {
  $("#myBtn").click(function () {
    $('.modal').modal('show');
  });

  $("#modalLong").click(function () {
    $('.modal').modal('show');
  });

  $("#modalScroll").click(function () {
    $('.modal').modal('show');
  });

  $('#modalCenter').click(function () {
    $('.modal').modal('show');
  });
});

// Popover Javascript

$(function () {
  $('[data-toggle="popover"]').popover()
});
$('.popover-dismiss').popover({
  trigger: 'focus'
});


// Version in Sidebar

var version = document.getElementById('version-ruangadmin');

version.innerHTML = "Version 1.0.1";


